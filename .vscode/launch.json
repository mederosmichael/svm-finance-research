{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run experiment from svm-finance-research",
      "type": "debugpy",
      "request": "launch",
      "program": "${file}",
      "cwd": "${workspaceFolder:svm-finance-research}",
      "console": "integratedTerminal",
      "env": {
        "PYTHONPATH": "${workspaceFolder:svm-finance-research}"
      }
    }
  ]
}
